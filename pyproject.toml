[project]
name = "zeitnetz-generator"
version = "1.0.0"
description = "Generalized Zeitnetz Generator — algorithmic time-grid composition after Lachenmann"
requires-python = ">=3.9"
dependencies = ["music21>=9.0"]

[project.scripts]
zeitnetz = "zeitnetz.cli:main"
